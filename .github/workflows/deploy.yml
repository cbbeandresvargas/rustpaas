#name: Deploy to RustPaaS

#on:
#  push:
#    branches:
#      - main
#  workflow_dispatch:

#env:
#  PROJECT_NAME: "mi-app" # Cambia esto por el nombre de tu app / subdominio
#  RUSTPAAS_URL: "https://paas.tudominio.com" # Cambia esto por la URL real de tu RustPaaS

#jobs:
#  build-and-deploy:
#    runs-on: ubuntu-latest
    
#    steps:
#      - name: Checkout Code
#        uses: actions/checkout@v4

#      - name: Cache Rust dependencies
#        uses: Swatinem/rust-cache@v2

#      - name: Install Rust Toolchain
#        uses: dtolnay/rust-toolchain@stable
#        with:
#          targets: x86_64-unknown-linux-gnu

#      - name: Build Release Binary
#        run: cargo build --release --target x86_64-unknown-linux-gnu

#      - name: Deploy to RustPaaS
#        run: |
#          # Encuentra el binario generado. Ajusta la ruta si tu Cargo.toml se llama distinto
#          BINARY_PATH="./target/x86_64-unknown-linux-gnu/release/${{ env.PROJECT_NAME }}"
          
#          echo "Deploying $BINARY_PATH to ${{ env.RUSTPAAS_URL }}..."
          
#          RESPONSE=$(curl -s -w "\nHTTP_STATUS:%{http_code}" -X POST "${{ env.RUSTPAAS_URL }}/api/deploy" \
#            -F "name=${{ env.PROJECT_NAME }}" \
#            -F "binary=@$BINARY_PATH")
            
#          HTTP_STATUS=$(echo "$RESPONSE" | tr -d '\n' | sed -e 's/.*HTTP_STATUS://')
#          BODY=$(echo "$RESPONSE" | sed -e 's/HTTP_STATUS\:.*//g')
          
#          if [ "$HTTP_STATUS" -eq 201 ]; then
#            echo "✅ Deploy exitoso!"
#            echo "$BODY"
#          else
#            echo "❌ Error en deploy (HTTP $HTTP_STATUS)"
#            echo "$BODY"
#            exit 1
#          fi
